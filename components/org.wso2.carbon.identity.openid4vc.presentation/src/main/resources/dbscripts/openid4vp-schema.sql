-- =====================================================================
-- OpenID4VP (OpenID for Verifiable Presentations) Database Schema
-- WSO2 Identity Server Integration
-- 
-- This script creates the required tables for storing VP requests,
-- submissions, and presentation definitions.
-- =====================================================================

-- ---------------------------------------------------------------------
-- Table: IDN_VP_REQUEST
-- Description: Stores VP (Verifiable Presentation) authorization requests
-- ---------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS IDN_VP_REQUEST (
    ID INTEGER AUTO_INCREMENT,
    REQUEST_ID VARCHAR(255) NOT NULL,
    TRANSACTION_ID VARCHAR(255) NOT NULL,
    CLIENT_ID VARCHAR(255) NOT NULL,
    NONCE VARCHAR(255) NOT NULL,
    PRESENTATION_DEFINITION_ID VARCHAR(255),
    PRESENTATION_DEFINITION TEXT,
    RESPONSE_URI VARCHAR(2048),
    RESPONSE_MODE VARCHAR(50) DEFAULT 'direct_post',
    REQUEST_JWT TEXT,
    STATUS VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',
    CREATED_AT BIGINT NOT NULL,
    EXPIRES_AT BIGINT NOT NULL,
    TENANT_ID INTEGER DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (REQUEST_ID, TENANT_ID),
    UNIQUE (TRANSACTION_ID, TENANT_ID)
);

-- Indexes for VP_REQUEST table
CREATE INDEX IDX_VP_REQ_TRANSACTION_ID ON IDN_VP_REQUEST(TRANSACTION_ID, TENANT_ID);
CREATE INDEX IDX_VP_REQ_STATUS ON IDN_VP_REQUEST(STATUS, TENANT_ID);
CREATE INDEX IDX_VP_REQ_EXPIRES ON IDN_VP_REQUEST(EXPIRES_AT, STATUS);
CREATE INDEX IDX_VP_REQ_CLIENT_ID ON IDN_VP_REQUEST(CLIENT_ID, TENANT_ID);


-- ---------------------------------------------------------------------
-- Table: IDN_VP_SUBMISSION
-- Description: Stores VP submissions (responses from wallets)
-- ---------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS IDN_VP_SUBMISSION (
    ID INTEGER AUTO_INCREMENT,
    SUBMISSION_ID VARCHAR(255) NOT NULL,
    REQUEST_ID VARCHAR(255) NOT NULL,
    VP_TOKEN TEXT,
    PRESENTATION_SUBMISSION TEXT,
    ERROR VARCHAR(255),
    ERROR_DESCRIPTION TEXT,
    VERIFICATION_STATUS VARCHAR(50),
    VERIFICATION_RESULT TEXT,
    SUBMITTED_AT BIGINT NOT NULL,
    TENANT_ID INTEGER DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (SUBMISSION_ID, TENANT_ID)
);

-- Indexes for VP_SUBMISSION table
CREATE INDEX IDX_VP_SUB_REQUEST_ID ON IDN_VP_SUBMISSION(REQUEST_ID, TENANT_ID);
CREATE INDEX IDX_VP_SUB_VERIFICATION ON IDN_VP_SUBMISSION(VERIFICATION_STATUS, TENANT_ID);


-- ---------------------------------------------------------------------
-- Table: IDN_PRESENTATION_DEFINITION
-- Description: Stores presentation definition templates
-- ---------------------------------------------------------------------
CREATE TABLE IF NOT EXISTS IDN_PRESENTATION_DEFINITION (
    ID INTEGER AUTO_INCREMENT,
    DEFINITION_ID VARCHAR(255) NOT NULL,
    NAME VARCHAR(255) NOT NULL,
    DESCRIPTION TEXT,
    DEFINITION_JSON TEXT NOT NULL,
    IS_DEFAULT BOOLEAN DEFAULT FALSE,
    CREATED_AT BIGINT NOT NULL,
    UPDATED_AT BIGINT,
    TENANT_ID INTEGER DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (DEFINITION_ID, TENANT_ID),
    UNIQUE (NAME, TENANT_ID)
);

-- Indexes for PRESENTATION_DEFINITION table
CREATE INDEX IDX_PRES_DEF_DEFAULT ON IDN_PRESENTATION_DEFINITION(IS_DEFAULT, TENANT_ID);


-- =====================================================================
-- PostgreSQL Specific Version
-- =====================================================================
-- If using PostgreSQL, use the following DDL instead:

/*
-- IDN_VP_REQUEST for PostgreSQL
CREATE TABLE IF NOT EXISTS IDN_VP_REQUEST (
    ID SERIAL,
    REQUEST_ID VARCHAR(255) NOT NULL,
    TRANSACTION_ID VARCHAR(255) NOT NULL,
    CLIENT_ID VARCHAR(255) NOT NULL,
    NONCE VARCHAR(255) NOT NULL,
    PRESENTATION_DEFINITION_ID VARCHAR(255),
    PRESENTATION_DEFINITION TEXT,
    RESPONSE_URI VARCHAR(2048),
    RESPONSE_MODE VARCHAR(50) DEFAULT 'direct_post',
    REQUEST_JWT TEXT,
    STATUS VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',
    CREATED_AT BIGINT NOT NULL,
    EXPIRES_AT BIGINT NOT NULL,
    TENANT_ID INTEGER DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (REQUEST_ID, TENANT_ID),
    UNIQUE (TRANSACTION_ID, TENANT_ID)
);

-- IDN_VP_SUBMISSION for PostgreSQL
CREATE TABLE IF NOT EXISTS IDN_VP_SUBMISSION (
    ID SERIAL,
    SUBMISSION_ID VARCHAR(255) NOT NULL,
    REQUEST_ID VARCHAR(255) NOT NULL,
    VP_TOKEN TEXT,
    PRESENTATION_SUBMISSION TEXT,
    ERROR VARCHAR(255),
    ERROR_DESCRIPTION TEXT,
    VERIFICATION_STATUS VARCHAR(50),
    VERIFICATION_RESULT TEXT,
    SUBMITTED_AT BIGINT NOT NULL,
    TENANT_ID INTEGER DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (SUBMISSION_ID, TENANT_ID)
);

-- IDN_PRESENTATION_DEFINITION for PostgreSQL
CREATE TABLE IF NOT EXISTS IDN_PRESENTATION_DEFINITION (
    ID SERIAL,
    DEFINITION_ID VARCHAR(255) NOT NULL,
    NAME VARCHAR(255) NOT NULL,
    DESCRIPTION TEXT,
    DEFINITION_JSON TEXT NOT NULL,
    IS_DEFAULT BOOLEAN DEFAULT FALSE,
    CREATED_AT BIGINT NOT NULL,
    UPDATED_AT BIGINT,
    TENANT_ID INTEGER DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (DEFINITION_ID, TENANT_ID),
    UNIQUE (NAME, TENANT_ID)
);

-- Indexes (same as above)
CREATE INDEX IDX_VP_REQ_TRANSACTION_ID ON IDN_VP_REQUEST(TRANSACTION_ID, TENANT_ID);
CREATE INDEX IDX_VP_REQ_STATUS ON IDN_VP_REQUEST(STATUS, TENANT_ID);
CREATE INDEX IDX_VP_REQ_EXPIRES ON IDN_VP_REQUEST(EXPIRES_AT, STATUS);
CREATE INDEX IDX_VP_REQ_CLIENT_ID ON IDN_VP_REQUEST(CLIENT_ID, TENANT_ID);
CREATE INDEX IDX_VP_SUB_REQUEST_ID ON IDN_VP_SUBMISSION(REQUEST_ID, TENANT_ID);
CREATE INDEX IDX_VP_SUB_VERIFICATION ON IDN_VP_SUBMISSION(VERIFICATION_STATUS, TENANT_ID);
CREATE INDEX IDX_PRES_DEF_DEFAULT ON IDN_PRESENTATION_DEFINITION(IS_DEFAULT, TENANT_ID);
*/


-- =====================================================================
-- Oracle Specific Version
-- =====================================================================
-- If using Oracle, use the following DDL instead:

/*
-- IDN_VP_REQUEST for Oracle
CREATE TABLE IDN_VP_REQUEST (
    ID NUMBER(10) GENERATED BY DEFAULT AS IDENTITY,
    REQUEST_ID VARCHAR2(255) NOT NULL,
    TRANSACTION_ID VARCHAR2(255) NOT NULL,
    CLIENT_ID VARCHAR2(255) NOT NULL,
    NONCE VARCHAR2(255) NOT NULL,
    PRESENTATION_DEFINITION_ID VARCHAR2(255),
    PRESENTATION_DEFINITION CLOB,
    RESPONSE_URI VARCHAR2(2048),
    RESPONSE_MODE VARCHAR2(50) DEFAULT 'direct_post',
    REQUEST_JWT CLOB,
    STATUS VARCHAR2(50) DEFAULT 'ACTIVE' NOT NULL,
    CREATED_AT NUMBER(19) NOT NULL,
    EXPIRES_AT NUMBER(19) NOT NULL,
    TENANT_ID NUMBER(10) DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (REQUEST_ID, TENANT_ID),
    UNIQUE (TRANSACTION_ID, TENANT_ID)
);

-- IDN_VP_SUBMISSION for Oracle
CREATE TABLE IDN_VP_SUBMISSION (
    ID NUMBER(10) GENERATED BY DEFAULT AS IDENTITY,
    SUBMISSION_ID VARCHAR2(255) NOT NULL,
    REQUEST_ID VARCHAR2(255) NOT NULL,
    VP_TOKEN CLOB,
    PRESENTATION_SUBMISSION CLOB,
    ERROR VARCHAR2(255),
    ERROR_DESCRIPTION CLOB,
    VERIFICATION_STATUS VARCHAR2(50),
    VERIFICATION_RESULT CLOB,
    SUBMITTED_AT NUMBER(19) NOT NULL,
    TENANT_ID NUMBER(10) DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (SUBMISSION_ID, TENANT_ID)
);

-- IDN_PRESENTATION_DEFINITION for Oracle
CREATE TABLE IDN_PRESENTATION_DEFINITION (
    ID NUMBER(10) GENERATED BY DEFAULT AS IDENTITY,
    DEFINITION_ID VARCHAR2(255) NOT NULL,
    NAME VARCHAR2(255) NOT NULL,
    DESCRIPTION CLOB,
    DEFINITION_JSON CLOB NOT NULL,
    IS_DEFAULT NUMBER(1) DEFAULT 0,
    CREATED_AT NUMBER(19) NOT NULL,
    UPDATED_AT NUMBER(19),
    TENANT_ID NUMBER(10) DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (DEFINITION_ID, TENANT_ID),
    UNIQUE (NAME, TENANT_ID)
);

-- Indexes for Oracle
CREATE INDEX IDX_VP_REQ_TRANSACTION_ID ON IDN_VP_REQUEST(TRANSACTION_ID, TENANT_ID);
CREATE INDEX IDX_VP_REQ_STATUS ON IDN_VP_REQUEST(STATUS, TENANT_ID);
CREATE INDEX IDX_VP_REQ_EXPIRES ON IDN_VP_REQUEST(EXPIRES_AT, STATUS);
CREATE INDEX IDX_VP_REQ_CLIENT_ID ON IDN_VP_REQUEST(CLIENT_ID, TENANT_ID);
CREATE INDEX IDX_VP_SUB_REQUEST_ID ON IDN_VP_SUBMISSION(REQUEST_ID, TENANT_ID);
CREATE INDEX IDX_VP_SUB_VERIFICATION ON IDN_VP_SUBMISSION(VERIFICATION_STATUS, TENANT_ID);
CREATE INDEX IDX_PRES_DEF_DEFAULT ON IDN_PRESENTATION_DEFINITION(IS_DEFAULT, TENANT_ID);
*/


-- =====================================================================
-- MS SQL Server Specific Version
-- =====================================================================
-- If using MS SQL Server, use the following DDL instead:

/*
-- IDN_VP_REQUEST for MS SQL
CREATE TABLE IDN_VP_REQUEST (
    ID INT IDENTITY(1,1),
    REQUEST_ID VARCHAR(255) NOT NULL,
    TRANSACTION_ID VARCHAR(255) NOT NULL,
    CLIENT_ID VARCHAR(255) NOT NULL,
    NONCE VARCHAR(255) NOT NULL,
    PRESENTATION_DEFINITION_ID VARCHAR(255),
    PRESENTATION_DEFINITION NVARCHAR(MAX),
    RESPONSE_URI VARCHAR(2048),
    RESPONSE_MODE VARCHAR(50) DEFAULT 'direct_post',
    REQUEST_JWT NVARCHAR(MAX),
    STATUS VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',
    CREATED_AT BIGINT NOT NULL,
    EXPIRES_AT BIGINT NOT NULL,
    TENANT_ID INT DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (REQUEST_ID, TENANT_ID),
    UNIQUE (TRANSACTION_ID, TENANT_ID)
);

-- IDN_VP_SUBMISSION for MS SQL
CREATE TABLE IDN_VP_SUBMISSION (
    ID INT IDENTITY(1,1),
    SUBMISSION_ID VARCHAR(255) NOT NULL,
    REQUEST_ID VARCHAR(255) NOT NULL,
    VP_TOKEN NVARCHAR(MAX),
    PRESENTATION_SUBMISSION NVARCHAR(MAX),
    ERROR VARCHAR(255),
    ERROR_DESCRIPTION NVARCHAR(MAX),
    VERIFICATION_STATUS VARCHAR(50),
    VERIFICATION_RESULT NVARCHAR(MAX),
    SUBMITTED_AT BIGINT NOT NULL,
    TENANT_ID INT DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (SUBMISSION_ID, TENANT_ID)
);

-- IDN_PRESENTATION_DEFINITION for MS SQL
CREATE TABLE IDN_PRESENTATION_DEFINITION (
    ID INT IDENTITY(1,1),
    DEFINITION_ID VARCHAR(255) NOT NULL,
    NAME VARCHAR(255) NOT NULL,
    DESCRIPTION NVARCHAR(MAX),
    DEFINITION_JSON NVARCHAR(MAX) NOT NULL,
    IS_DEFAULT BIT DEFAULT 0,
    CREATED_AT BIGINT NOT NULL,
    UPDATED_AT BIGINT,
    TENANT_ID INT DEFAULT -1234,
    PRIMARY KEY (ID),
    UNIQUE (DEFINITION_ID, TENANT_ID),
    UNIQUE (NAME, TENANT_ID)
);

-- Indexes for MS SQL
CREATE INDEX IDX_VP_REQ_TRANSACTION_ID ON IDN_VP_REQUEST(TRANSACTION_ID, TENANT_ID);
CREATE INDEX IDX_VP_REQ_STATUS ON IDN_VP_REQUEST(STATUS, TENANT_ID);
CREATE INDEX IDX_VP_REQ_EXPIRES ON IDN_VP_REQUEST(EXPIRES_AT, STATUS);
CREATE INDEX IDX_VP_REQ_CLIENT_ID ON IDN_VP_REQUEST(CLIENT_ID, TENANT_ID);
CREATE INDEX IDX_VP_SUB_REQUEST_ID ON IDN_VP_SUBMISSION(REQUEST_ID, TENANT_ID);
CREATE INDEX IDX_VP_SUB_VERIFICATION ON IDN_VP_SUBMISSION(VERIFICATION_STATUS, TENANT_ID);
CREATE INDEX IDX_PRES_DEF_DEFAULT ON IDN_PRESENTATION_DEFINITION(IS_DEFAULT, TENANT_ID);
*/
